List of JavaScript functions:
----------------------------
makeGetRequest
makePostRequest


validateInfo <- this is added to the submit buttons via event listener
	if(buttonClass contains '.button-page1' && accountType == 'student')
		go to page 2
		
	elif (accountType == 'student')
		createStudentAccount
		
	elif (accountType == 'instructor)
		createInstructorAccount
	


createInstructorAccount (calls storeAccountData)
createStudentAccount (calls storeAccountData)

confirmLogin (calls makeGetRequest)

-----------------------------
displayData (what to do about pages needing different information?)

